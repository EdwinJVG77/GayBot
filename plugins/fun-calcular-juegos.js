let handler = async (m, { conn, command, text, usedPrefix }) => {
if (!text) throw `${lenguajeGB['smsAvisoMG']()}*𝗘𝗧𝗜𝗤𝗨𝗘𝗧𝗘 @𝗧𝗔𝗚 𝗢 𝗘𝗦𝗖𝗥𝗜𝗕𝗔 𝗘𝗟 𝗡𝗢𝗠𝗕𝗥𝗘*\n*𝗧𝗔𝗚𝗦𝗢𝗠𝗘𝗢𝗡𝗘 @𝗧𝗔𝗚 𝗢𝗥 𝗧𝗬𝗣𝗘 𝗧𝗛𝗘 𝗡𝗔𝗠𝗘*` 

if (command == 'gay2') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% GAY 🏳️‍🌈*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

 // ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'lesbiana') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🏳️‍🌈*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pajero') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🍆💦*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pajera') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🍑💦`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'puto') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()}, MÁS INFORMACIÓN A SU PRIVADO🔥🥵 XD*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'puta') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()}, MÁS INFORMACIÓN A SU PRIVADO🔥🥵 XD*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

 // ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'manco') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 💩*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------  
  
if (command == 'manca') {
let juego = `*${text.toUpperCase()} ES/ IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 💩*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------ 
  
if (command == 'rata') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🐁 COME QUESO 🧀*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------   
  
if (command == 'prostituto') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🍆🙉, QUIEN QUIERE DE SUS SERVICIOS? XD*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
// ------------------------------------------------------------------------------------------------------------------------------------------------   
  
if (command == 'prostituta') {
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 🍑🙉, QUIEN QUIERE DE SUS SERVICIOS? XD*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
  
 // ------------------------------------------------------------------------------------------------------------------------------------------------   
if (command == 'love2') {
let juego = `*EL AMOR DE ${text.toUpperCase()} POR TI ES DE ${(500).getRandom()}% DEBERIAS PEDIRLE QUE SEA TU NOVIO/A 💕*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pendejo') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 😹*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'pendeja') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 😹*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'insano') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 👻*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'insana') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 👻*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------------------------------------------------------------------------------------------------------------------------------------
  
if (command == 'imbecil') { 
let juego = `*${text.toUpperCase()} ES/IS ${(500).getRandom()}% ${command.replace('how', '').toUpperCase()} 😹*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}
}
handler.help = ['love', 'gay2', 'lesbiana', 'pajero', 'pajera', 'puto', 'puta', 'manco', 'manca', 'rata', 'prostituta', 'prostituto'].map(v => v + ' @tag | nombre')
handler.tags = ['calculator']
handler.command = /^love2|gay2|lesbiana|pajero|pajera|puto|puta|manco|manca|rata|prostituta|prostituto|pendejo|pendeja|insano|insana|imbecil/i
handler.exp = 100
handler.money = false
export default handler
